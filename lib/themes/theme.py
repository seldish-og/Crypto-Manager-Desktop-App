class Theme:
	def __init__(self, backgroundColor, chartLineColor, gridLineColor, currentPriceLineColor, bearColor, bullColor, textColor) -> None:
		self.backgroundColor = backgroundColor
		self.chartLineColor = chartLineColor
		self.gridLineColor = gridLineColor
		self.currentPriceLineColor = currentPriceLineColor
		self.bearColor = bearColor
		self.bullColor = bullColor
		self.textColor = textColor